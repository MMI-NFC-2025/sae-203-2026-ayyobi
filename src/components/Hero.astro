---
const {
  title,
  subtitle,
  buttonText,
  buttonHref = "#",
  image,
  dates = [],
} = Astro.props;
---

<section class="relative isolate overflow-hidden">
  <img
    src={image}
    alt={title}
    class="absolute inset-0 -z-10 h-full w-full object-cover"
  />
  <div class="absolute inset-0 -z-10 bg-[#1b2230]/55"></div>

  <div class="mx-auto max-w-7xl px-4 py-16 md:px-6 md:py-24 lg:py-28">
    <div class="mx-auto max-w-3xl text-center">
      <h1
        class="font-mono text-5xl font-bold leading-none text-white md:text-7xl"
      >
        {title}
      </h1>

      <p
        class="mx-auto mt-6 max-w-2xl text-center text-lg leading-8 text-white/85 md:text-2xl"
      >
        {subtitle}
      </p>

      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <a
          href={buttonHref}
          class="inline-flex w-full items-center justify-center rounded-full bg-[#facc15] px-8 py-4 text-base font-semibold text-black transition hover:opacity-90 sm:w-auto"
        >
          {buttonText}
        </a>
      </div>
    </div>

    {
      dates.length > 0 && (
        <div class="mx-auto mt-12 grid gap-4 justify-items-center sm:grid-cols-2 lg:grid-cols-4 xl:max-w-6xl">
          {dates.map((item) => (
            <article class="flex min-h-[120px] w-full max-w-[480px] flex-col justify-center rounded-[22px] border border-white/15 bg-[#1b2230]/60 p-5 text-center backdrop-blur-sm">
              <h2 class="font-mono text-xl font-bold text-[#facc15]">
                {item.day}
              </h2>
              <p class="mt-2 text-sm text-white/80">{item.text}</p>
            </article>
          ))}
        </div>
      )
    }
  </div>
</section>
